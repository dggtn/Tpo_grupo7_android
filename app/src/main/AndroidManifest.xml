<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tpo_mobile"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- MainActivity - Pantalla de login -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Tpo_mobile">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AppGymActivity - Aplicación principal del gym -->
        <activity
            android:name=".activities.AppGymActivity"
            android:exported="false"
            android:theme="@style/Theme.Tpo_mobile"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".activities.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MainActivity" />
        </activity>

        <!-- ErrorHandlerActivity - Pantalla de manejo de errores -->
        <activity
            android:name=".activities.ErrorHandlerActivity"
            android:exported="false"
            android:theme="@style/Theme.Tpo_mobile"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:finishOnTaskLaunch="true">
            <!--
            Explicación de los atributos:
            - launchMode="singleTop": Evita múltiples instancias de la pantalla de error
            - taskAffinity="": Crea una nueva tarea para la pantalla de error
            - excludeFromRecents="true": No aparece en la lista de apps recientes
            - finishOnTaskLaunch="true": Se cierra cuando se inicia otra tarea
            -->
        </activity>

    </application>

</manifest>